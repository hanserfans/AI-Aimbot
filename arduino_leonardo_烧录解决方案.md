# Arduino Leonardo 烧录连接问题解决方案

## 🔍 问题诊断
您遇到的是典型的Arduino Leonardo烧录连接问题：
```
avrdude: butterfly_recv(): programmer is not responding
avrdude: ser_recv(): read error: 设备无法识别
```

## 🎯 解决方案

### 方法1: Reset按钮时序法 (推荐)
1. **准备阶段**:
   - 确保Arduino IDE已打开固件文件
   - 选择正确的板子: Arduino Leonardo
   - 选择正确的端口: COM6

2. **烧录步骤**:
   - 点击Arduino IDE的"上传"按钮
   - **立即**按住Arduino板上的Reset按钮
   - 等待IDE显示"正在上传..."
   - **立即**松开Reset按钮
   - 等待烧录完成

### 方法2: 双击Reset法
1. **快速双击**Arduino板上的Reset按钮
2. 在双击后的**8秒内**点击Arduino IDE的上传按钮
3. 此时Arduino会进入bootloader模式，更容易接受烧录

### 方法3: 手动进入Bootloader模式
1. 按住Reset按钮
2. 点击Arduino IDE的上传按钮
3. 当IDE显示"正在连接到程序员"时，松开Reset按钮
4. 立即再次短按Reset按钮

## 🔧 高级解决方案

### 检查USB连接
- 确保使用**数据线**而非充电线
- 尝试更换USB端口
- 检查USB线是否损坏

### Arduino IDE设置
- 工具 → 板子 → Arduino Leonardo
- 工具 → 端口 → COM6 (Arduino Leonardo)
- 工具 → 程序员 → AVRISP mkII

### 如果仍然失败
1. 重启Arduino IDE
2. 重新插拔USB线
3. 重启计算机
4. 检查驱动程序是否正确安装

## 🎯 成功标志
烧录成功时您会看到：
```
Sketch uses 8292 bytes (28%) of program storage space.
Global variables use 421 bytes (16%) of dynamic memory.
Done uploading.
```

## 🧪 烧录后测试
烧录完成后运行：
```bash
python test_arduino_connection.py
```

应该显示：
- ✅ Arduino连接成功
- ✅ 基础功能测试通过