# AI-Aimbot 鼠标控制机制分析

## 🖱️ 鼠标控制方式概述

AI-Aimbot 项目使用多种鼠标控制方式，以适应不同的使用场景和反检测需求：

### 1. 标准 Win32 API 控制（主要方式）
**使用文件**: `main_onnx.py`, `main.py`, `main_tensorrt.py` 等主要脚本

**控制方式**:
```python
import win32api
import win32con

# 鼠标移动实现
if win32api.GetKeyState(0x14):  # 检测 Caps Lock 键状态
    win32api.mouse_event(win32con.MOUSEEVENTF_MOVE, 
                        int(mouseMove[0] * aaMovementAmp), 
                        int(mouseMove[1] * aaMovementAmp), 0, 0)
```

**特点**:
- ✅ 直接调用 Windows API
- ✅ 响应速度快
- ✅ 兼容性好
- ⚠️ 可能被某些反作弊系统检测

### 2. Logitech G-Hub 驱动控制（高级方式）
**使用文件**: `customScripts/main_onnx.py`, `customScripts/Villageslayer/main_onnx.py` 等

**控制方式**:
```python
from mouse_driver.MouseMove import mouse_move as ghub_move

# 使用 G-Hub 驱动移动鼠标
if win32api.GetKeyState(0x02) < 0:  # 检测右键状态
    ghub_move(mouseMove[0], mouseMove[1])
```

**特点**:
- ✅ 通过硬件驱动层控制
- ✅ 更难被反作弊系统检测
- ✅ 模拟真实硬件行为
- ✅ 需要 Logitech 鼠标和 G-Hub 软件
- ✅ **已完成升级实现** - `mouse_driver` 模块已成功集成
- 💡 **实现基础**: 基于 [G-Input 项目](https://github.com/xxreflextheone/g-input) 的稳定代码

### 3. 外部硬件控制（最安全方式）
**使用文件**: `customScripts/afyScripts/afy_raspberry_pi_pico_w_tensorrt.py`

**控制方式**:
```python
import socket

# 通过网络发送命令到外部硬件设备
def moveafy(x, y):
    if x != 0 or y != 0:
        command = f'M{x},{y}\r'
        client.sendall(command.encode())
```

**特点**:
- ✅ 完全物理隔离
- ✅ 无法被软件检测
- ✅ 使用真实硬件设备（如树莓派）
- ⚠️ 需要额外硬件设备
- ⚠️ 设置复杂度较高

## 🔧 触发机制

### 激活键位
- **Caps Lock (0x14)**: 主要激活键，用于标准 Win32 API 控制
  - 📍 **物理位置**: 键盘左侧，字母 A 键的正上方
  - 🔤 **标识**: 通常标有 "Caps Lock" 或 "⇪" 符号
  - 💡 **指示**: 按下后键盘 Caps Lock 指示灯会亮起
- **右键 (0x02)**: 用于 G-Hub 驱动控制
- **自定义键位**: 可在 `config.py` 中通过 `aaQuitKey` 配置

### 移动计算
```python
# 计算鼠标移动距离
mouseMove = [xMid - cWidth, (yMid - headshot_offset) - cHeight]

# 应用移动幅度调节
finalMove = [int(mouseMove[0] * aaMovementAmp), int(mouseMove[1] * aaMovementAmp)]
```

## ⚠️ 反检测机制

### 1. 移动幅度控制
- **配置参数**: `aaMovementAmp = 0.4`
- **作用**: 降低移动幅度，模拟人类操作
- **效果**: 避免过于精确的机械式移动

### 2. 爆头模式偏移
```python
if headshot_mode:
    headshot_offset = box_height * 0.38  # 爆头偏移
else:
    headshot_offset = box_height * 0.2   # 身体偏移
```
- **作用**: 添加随机性，避免总是瞄准同一位置

### 3. 帧率控制
- **屏幕截图尺寸**: 320x320 像素
- **置信度阈值**: 0.4
- **作用**: 平衡性能和检测精度

## 🚨 已知检测风险

### 高风险游戏
根据 README.md 记录：
- **Splitgate**: 已被用户报告检测
- **EQU8**: 检测 win32 鼠标移动库

### 检测原理
1. **API 调用监控**: 反作弊系统监控 `win32api.mouse_event` 调用
2. **移动模式分析**: 检测过于规律或精确的鼠标移动
3. **响应时间分析**: 检测超人类反应速度

## 🛡️ 安全建议

### 1. 选择合适的控制方式
- **低风险**: 使用外部硬件控制
- **中风险**: 使用 G-Hub 驱动控制
- **高风险**: 使用标准 Win32 API 控制

### 2. 调整配置参数
```python
# 在 config.py 中调整以下参数
aaMovementAmp = 0.3      # 降低移动幅度
confidence = 0.5         # 提高置信度阈值
headshot_mode = False    # 关闭爆头模式增加随机性
```

### 3. 使用时机选择
- 避免在竞技模式使用
- 选择反作弊系统较松的游戏
- 定期更换使用模式

## 📊 当前配置状态

根据您的系统配置：
- **GPU**: NVIDIA GeForce RTX 4060 Laptop GPU
- **CUDA**: 12.4
- **ONNX 选择**: 3 (NVIDIA GPU 加速)
- **移动幅度**: 0.4
- **置信度**: 0.4
- **爆头模式**: 启用
- **控制方式**: Win32 API (标准方式)

## 🔄 升级建议

### 1. 安装 G-Hub 驱动控制
```bash
# 需要安装 mouse_driver 模块
# 注意：当前项目中该模块缺失，需要单独获取
```

### 2. 配置外部硬件控制
- 准备树莓派或类似设备
- 配置网络连接
- 修改 IP 和端口设置

### 3. 优化检测参数
- 根据具体游戏调整 `aaMovementAmp`
- 测试不同的 `confidence` 值
- 实验不同的触发键位

## ⚖️ 使用责任

**重要提醒**:
- 本工具仅供学习和研究目的
- 在多人游戏中使用可能违反游戏条款
- 用户需自行承担使用风险
- 建议在单机或允许的环境中测试

---

*最后更新: 2024年12月*
*项目状态: 完全配置并可运行*